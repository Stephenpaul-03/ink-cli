export const defaultConfig = {
  scopes: {},
  aliases: {},
  synonyms: {},
  historyLimit: 10,
  emoji: false,
  gitmoji: {},
  theme: "minimal",
  color: true,
  template: {
    header: "{preserved} {type}({scope}): {desc}",
  },
};
